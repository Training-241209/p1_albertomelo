

export function TitleHeader() {
    return (
        
      <h1 className="text-4xl font-bold bg-gradient-to-r from-blue-400 via-purple-500 to-white bg-clip-text text-transparent tracking-tight text-center drop-shadow-sm">
        Employee Reimbursement System
      </h1>
        /* 
      Sleek System Title:
      - `text-4xl`: Sets the font size to 4xl (large text).
      - `font-bold`: Makes the text bold.
      - `bg-gradient-to-r`: Creates a background gradient that flows from left to right.
      - `from-blue-400 via-purple-500 to-white`: Defines the gradient colors, transitioning from blue to purple to white.
      - `bg-clip-text`: Clips the background to the shape of the text, allowing the gradient to show only inside the text.
      - `text-transparent`: Makes the text color transparent so the gradient is visible.
      - `tracking-tight`: Reduces the letter spacing for tighter text.
      - `text-center`: Centers the text horizontally.
      - `drop-shadow-sm`: Adds a small drop shadow to the text for a subtle effect.
      */
    );
  }
  